services:

  # ── PostgreSQL ────────────────────────────────────────────────────────────
  db:
    image: postgres:18-alpine
    container_name: easm-db
    restart: unless-stopped
    labels:
      - easm.logs=enabled
      - easm.service=db
    environment:
      POSTGRES_USER: easm
      POSTGRES_PASSWORD: ${EASM_DB_PASSWORD:-easm-dev-password}
      POSTGRES_DB: easm
    volumes:
      - easm-db:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U easm -d easm"]
      interval: 5s
      timeout: 3s
      retries: 12
    networks:
      - easm

  # ── Redis (Scan-Queue) ────────────────────────────────────────────────────
  redis:
    image: redis:8-alpine
    container_name: easm-redis
    restart: unless-stopped
    labels:
      - easm.logs=enabled
      - easm.service=redis
    volumes:
      - easm-redis:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 12
    networks:
      - easm

  # ── FastAPI Backend ──────────────────────────────────────────────────────
  backend:
    build: ./app/backend
    container_name: easm-backend
    restart: unless-stopped
    labels:
      - easm.logs=enabled
      - easm.service=backend
    volumes:
      - easm-results:/results
      - easm-data:/data
    environment:
      - PYTHONUNBUFFERED=1
      - TZ=${TZ:-UTC}
      - DATABASE_URL=postgresql+psycopg://easm:${EASM_DB_PASSWORD:-easm-dev-password}@db:5432/easm
      - REDIS_URL=redis://redis:6379/0
      - DOCKER_PROXY_URL=http://socket-proxy:2375
      - EASM_ADMIN_PASSWORD=${EASM_ADMIN_PASSWORD:-}
      - EASM_ADMIN_PASSWORD_HASH=${EASM_ADMIN_PASSWORD_HASH:-}
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/auth/check"]
      interval: 5s
      timeout: 3s
      retries: 20
    networks:
      - easm
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy

  # ── Scan Worker (RQ) ──────────────────────────────────────────────────────
  worker:
    build: ./app/backend
    container_name: easm-worker
    restart: unless-stopped
    command: rq worker-pool easm-scans --num-workers 2 --url redis://redis:6379/0
    labels:
      - easm.logs=enabled
      - easm.service=worker
    volumes:
      - easm-results:/results
      - easm-data:/data
    environment:
      - PYTHONUNBUFFERED=1
      - TZ=${TZ:-UTC}
      - DATABASE_URL=postgresql+psycopg://easm:${EASM_DB_PASSWORD:-easm-dev-password}@db:5432/easm
      - REDIS_URL=redis://redis:6379/0
    networks:
      - easm
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    mem_limit: 2g
    cpus: 1.5

  # ── React Frontend via nginx ─────────────────────────────────────────────
  frontend:
    build: ./app/frontend
    container_name: easm-frontend
    restart: unless-stopped
    labels:
      - easm.logs=enabled
      - easm.service=frontend
    volumes:
      - easm-tls:/etc/nginx/tls
    environment:
      - EASM_TLS=${EASM_TLS:-on}
      - EASM_TLS_SAN=${EASM_TLS_SAN:-}
      - EASM_HSTS=${EASM_HSTS:-off}
    ports:
      - "3000:80"
      - "3443:443"
    depends_on:
      backend:
        condition: service_healthy
    networks:
      - easm

  # ── Docker Socket Proxy (read-only container log access) ─────────────────
  # wollomatic/socket-proxy: regex allowlists per HTTP method; every method not
  # listed is denied, so GET-only below means POST/PUT/DELETE are blocked.
  # The socket is mounted :ro here only — never into app containers.
  socket-proxy:
    image: wollomatic/socket-proxy:1.12.3
    container_name: easm-socket-proxy
    restart: unless-stopped
    command:
      - "-listenip=0.0.0.0"
      - "-allowfrom=backend"
      - "-allowGET=/containers/json"
      - "-allowGET=/containers/[^/]+/logs"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    networks:
      - easm

volumes:
  easm-results:
  easm-data:
  easm-tls:
  easm-db:
  easm-redis:

networks:
  easm:
    driver: bridge
